{
  "name": "hours-ledger-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute hours-ledger --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.34.0"
  }
}
