# Set these in the GitHub Pages build environment after deploying the Worker.
# Include https:// and do not include /api/entries.
VITE_API_URL=https://hours-ledger-api.<your-account>.workers.dev
VITE_API_TOKEN=replace-with-the-worker-api-token
